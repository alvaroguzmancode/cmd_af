<% content_for :content do %>
    <div class="panel panel-default">
        <div class="panel-body" style="padding-right:0;padding-left:0;">
            <table class="table table-striped">
                <thead>
                    <tr>
                    <th>Descripcion</th>
                    <th colspan="3"></th>
                    </tr>
                </thead>

                <tbody>
                    <% @tipo_equipos.each do |tipo| %>
                    <tr>
                        <td><%= tipo.descripcion %></td>
                        <td><%= link_to 'Show', tipo, {:class=>"btn btn-primary btn-sm"} %></td>
                        <td><%= link_to 'Edit', edit_tipo_equipo_path(tipo), {:class=>"btn btn-success btn-sm"} %></td>
                        <td><%= link_to 'Destroy', tipo, method: :delete, data: { confirm: 'Are you sure?'}, :class=>'btn btn-warning btn-sm' %></td>
                    </tr>
                    <% end %>
                </tbody>
            </table>
        </div>

        <div class="panel-footer">
            <div class="digg_pagination">
                <div class="page_info">
                <%= page_entries_info @tipo_equipos %>
                </div>
                <%= will_paginate @tipo_equipos, :container => false %>
            </div>
        </div>

    </div>

<% end %>

<% content_for :title do %> 
    Listado <%= link_to 'New','tipo_equipos/new', {:class=>"btn btn-info btn-sm",:style=>"float:right;margin-top:-10;"} %> 
<% end %>

<%= render template: 'layouts/appb' %>